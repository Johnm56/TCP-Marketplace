A simple client-server marketplace application built using Python and TCP sockets.  
Multiple clients can connect to a server to view, buy, and sell items.

This project demonstrates basic network programming using TCP.  
The server manages marketplace data, while clients send requests to interact with the system.

- Multiple clients can connect to the server
- View available products
- Buy items
- Add items for sale
- Server updates inventory in real time
